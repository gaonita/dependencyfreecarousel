import React from 'react';

const Header = () => {
    return (
        <header>
            <p className={"headerText"}>Dependency-free Carousel</p>
        </header>
    );
};

export default Header;